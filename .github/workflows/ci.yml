name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4.2.2
      - uses: astral-sh/setup-uv@v5  # v5
        with:
          version: "latest"
      - name: Ruff lint
        run: uvx ruff check app/ tests/
      - name: Ruff format check
        run: uvx ruff format --check app/ tests/

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4.2.2
      - uses: astral-sh/setup-uv@v5  # v5
      - name: Install dependencies
        run: uv venv && uv pip install -e ".[dev]"
      - name: mypy
        run: uv run mypy app/

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4.2.2
      - name: Install graphviz
        run: sudo apt-get install -y graphviz
      - uses: astral-sh/setup-uv@v5  # v5
      - name: Install dependencies
        run: uv venv && uv pip install -e ".[dev]"
      - name: Run tests with coverage
        run: uv run pytest tests/ --cov=app --cov-report=xml --cov-report=term-missing -v
      - name: Upload coverage report
        uses: actions/upload-artifact@v4  # v4.6.1
        with:
          name: coverage-report
          path: coverage.xml

  build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4.2.2
      - name: Build Docker image
        run: docker build -t healthcheck-api:ci .
      - name: Verify image runs
        run: |
          docker run -d --name test-container -p 8080:8080 healthcheck-api:ci
          sleep 10
          curl -f http://localhost:8080/health
          docker stop test-container

  terraform-validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4.2.2
      - uses: hashicorp/setup-terraform@v3  # v3.1.2
        with:
          terraform_version: "~1.5"
      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        working-directory: terraform
      - name: Terraform Init (local backend)
        run: terraform init -backend=false
        working-directory: terraform
      - name: Terraform Validate
        run: terraform validate
        working-directory: terraform
